apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-blue
  namespace: sandeep
spec:
  replicas: 3
  selector:
    matchLabels:
      app: blue
  template:
    metadata:
      labels:
        app: blue
    spec:
      containers:
      - name: c1
        image: caddy
